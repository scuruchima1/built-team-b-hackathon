import React from 'react';
import './map.css'; // Assuming you'll have some styles for the header

const DisplayMap = () => {
    return (
        <div className="displayMap">
            <h1>place holder for map</h1>
        </div>
    );
};

export default DisplayMap;